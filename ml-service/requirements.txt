scikit-learn==1.6.1
joblib>=1.3.0
numpy>=2.0,<3.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pillow>=10.3.0
python-multipart>=0.0.9

# Explains which inputs drove a crop recommendation. If it is absent the
# prediction still works - the "Why this crop" panel is simply omitted.
shap>=0.46
