# Normalise line endings so the repo is identical on Windows, macOS and Linux.
* text=auto eol=lf

# Binary artefacts: never touch these.
*.pkl   binary
*.h5    binary
*.keras binary
*.png   binary
*.jpg   binary
*.webp  binary
*.ipynb binary
